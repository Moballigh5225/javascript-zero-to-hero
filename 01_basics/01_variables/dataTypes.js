"use strict"
// alert(3 + 3) we are using Node.js not browser thats why it will throw error 

const name ="mobi";
const age = 54;
const alive = true;

// primitive DataTypes

// number = 2 to power 53 range
// string
// boolean
// bigint = for bigger numbers
// null => stand alone value
// undefined =>
// symbol => unique

// type of undefined is undefined so its already a type
// but the type of null is object
// undefined means a variable has been declared but has not yet been assigned a value, whereas null is an assignment value, meaning that a variable has been declared and given the value of null .